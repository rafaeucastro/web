export const TaskDBTables = Object.freeze({
  tasks: "tasks",
});

export const TransactionMode = Object.freeze({
  readonly: "readonly",
  readwrite: "readwrite",
});

export const ErrorCodeEnum = Object.freeze({
  notFound: 8,
});